"""Mars Manifest Tool — catalog-driven flight-manifest and campaign planner.

See HANDOFF.md for the full specification. Engines are pure and tested;
reports only render; assumptions are injected, never hard-coded.
"""

__version__ = "0.1.0"
